# Frozen new-year evaluation protocol — 11 September 2026

Frozen before acquiring or inspecting2024/2025weather values. Training2023 and reused development2022 are closed for further tuning. Primary comparison: all27 method-union policies (SAA24, minimax24, reduced24→8 ×3seeds ×3arms), no reselection. BESS policies each use both frozen monthly-climatology and monthly/current-solar controllers from causal_forecasts_v2; noBESS has one equivalent controller. Secondary deterministic baseline: existing capped-union mean policies8,16,24, on their original pools. These deterministic policies are not pool-matched to method_union, so comparisons to them are explicitly secondary and potentially confounded by column availability. Do not attribute such differences purely to stochastic optimization. No policy is refit on new years.

100cases total: (18BESS×2controllers +9noBESS +2meanBESS×2controllers +1meanNoBESS)×2years. Evaluate entire2024(366days) and2025(365days) separately, same requested site33.25N,-119.5W and fixedUTC−7 (Etc/GMT+7). Use Open-Meteo historical archive default Best Match with hourly shortwave_radiation. This exactly reproduced all8760training2023values in the audit; explicit ERA5 did not. Record requested and returned coordinates, model selection, units, timezone, raw response hashes and transformed CSV hashes. Source: https://open-meteo.com/en/docs/historical-weather-api . Best Match may combine products; this is not a homogeneous ERA5 study or independent weather-station validation.

Preserve benchmark hour labels exactly: hourh of the returned series goes to indexed benchmark hourh and is split evenly into two half-hour slots. Open-Meteo defines shortwave radiation as the mean of the preceding hour; therefore this continuation preserves the submitted benchmark indexing, not a validated physical start-of-hour interpretation or actual Los Angeles DST timetable. FixedUTC−7 avoids variable-length local days. All operational-time claims require a separate corrected-clock/retrained study. Current-slot average is assumed observed at dispatch, an optimistic information assumption.

PV scale remains learned from2023only. All truck routes, integer assets and common initial BESS state remain frozen. BESS hard cyclic terminal. Controller LP minimizes diagnostic emergency supply, then cost; any daily shortage>1e-6 fails original-model feasibility. Compare full-day foresight oracle to both causal controllers on the same dates. Report all-day failure rates, shortages and distributions; conditional costs on jointly feasible dates only, with denominators. No zero substitution. Primary statistics: peryear/policy paired14-day circular block bootstrap2000replicates,seed7291; separate years, not pooled scenario seeds as independent years. Cross-year differences reported descriptively. Zero observed failures is not a zero population-risk certificate. MIP gaps and finite-pool/provenance limits remain attached; evaluation creates no new optimality certificate.

Data QA gates: valid JSON; correct offset−25200 and unitsW/m²; all expected contiguous hourly timestamps, no duplicates/missing/null/negative/nonfinite GHI;365/366 complete24-hourdays; exact requested year; manifest hashes. No outcome-based filtering or year replacement. If API unavailable/mismatched, stop acquisition, retain error, do not switch sources silently. All100cases report regardless of performance. Fresh means untouched in this research workflow; it does not assert no other researcher ever viewed these public data.

No RL, hyperparameter tuning or new optimization on these years. New code/outputs isolated; default_partition concurrency50,requeue,dailyjournals, explicit shared Slurm client config, reserved-node exclusion. No MIPs required; nominal100CPU-hour ceiling at1hour/case before retries, expected much smaller. Next research decision follows this frozen validation, not another development sweep.

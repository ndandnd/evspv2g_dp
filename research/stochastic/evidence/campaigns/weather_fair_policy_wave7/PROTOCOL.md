# Matched deterministic and minimax tie-break control

Frozen 12 September 2026 before aggregating the completed 2024/2025 validation. This follows the weaknesses recorded in the briefing, not selection based on new-year outcomes. Fit only2023; evaluate only2022development in this new campaign. The original frozen100-case holdout stays unchanged and is analyzed separately.

30policies:3arms(solar+BESS,V2G/noBESS,V2G+BESS) × [one annual-mean deterministic policy + three seeds(11,29,47) × SAA24 / minimax24 / minimax24 with expected-cost tie-break]. All10policies perarm use exactly the same existing validated method-union pool, with no new columns. Shared x,N,s0 and full-day continuous training recourse; mandatory trips unchanged. No RL/adaptive truck charging.

All MIPs have900seconds total native Gurobi optimization budget, one thread and seed as configured. For tie-break cases, stage1 solves fixed cost plus maximum sampled recourse, with450seconds ceiling and relativegap1e-6. Stage2 minimizes fixed cost plus mean recourse subject to the primary objective <= stage1incumbent+1e-5; its budget is900 minus stage1Runtime. Pureminimax controls use the same primarygap1e-6 and900seconds. SAA/mean usegap1e-6 and900seconds. Do not call a time-limited stage1 lexicographically optimal: report primary incumbent,bound,gap and absolute1e-5 allowance. Stage2 is a new constrained finite-pool expected-cost problem; its bound is not a global expected-cost/full-route bound. No branch-and-bound restart is called continuation.

Falsifiers: if the large mean-cost penalty disappears after the tie-break, attribute the earlier finding partly to primary-objective degeneracy rather than intrinsic robust conservatism. If deterministic advantages/disadvantages change on matched pools, correct the earlier unmatched comparison. Report allseeds, gaps, nativebudget and complete failures, even when a result is unfavorable. No blanket reruns to tighten gaps without a specific unresolved claim.

50causal evaluations:20BESS policies×both frozen monthly/current-solar forecasts plus10noBESS with the equivalent monthly controller. Daily2022oracle paired with causal dispatch; all-day failure/energy-shortage statistics, conditional feasible-day cost clearly labeled;2000replicate14-day circular blocks,seed7291. Frozen truck profiles, hardcyclicBESS and current-slot-average observation assumption unchanged.

Prepare three pools on default_partition with requeue; MIP30onScaglione no-requeue; evaluation50ondefault_partition requeue. All1CPU,4GiBforprep/MIP and2GiBforeval;reservedGPUhostexcluded;explicit shared Slurm client config. Daily eval hashchains/fsync,atomicstatus,identity-checkedresume. Scientificworker is a new immutable release; sourcecode/pools/data untouched. Retain native logs and environment metadata. Resource ceilings:prep3×30min, MIP30×20min(7.5native solverCPUhours), eval50×1h; actual usage measured, not inferred from ceilings.

#!/bin/bash
#SBATCH --job-name=evsp_overnight10
#SBATCH --output=overnight10_%j.out
#SBATCH --error=overnight10_%j.err
#SBATCH --cpus-per-task=4
#SBATCH --mem=16G
#SBATCH --requeue
# no --time: studies are sized to ~5-8 h; the partition default applies.

set -euo pipefail

cd "$HOME/evspv2g_dp"
mkdir -p logs results

export GRB_LICENSE_FILE=/share/apps/software/gurobi/gurobi.lic
unset LM_LICENSE_FILE
export OVERNIGHT10_STUDIES="${OVERNIGHT10_STUDIES:-SCHED3,THEORY,STOCH}"
export OVERNIGHT10_SHARD="${OVERNIGHT10_SHARD:-0/1}"

source /share/apps/software/anaconda3/etc/profile.d/conda.sh
conda activate /home/nc437/evsp_env

python3 -c "import gurobipy; print('gurobi', gurobipy.gurobi.version())"
python3 overnight10.py
